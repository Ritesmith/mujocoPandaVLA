# 仿真器评估对比报告

> 评估对象：ManiSkill3、Isaac Lab、MuJoCo（当前使用）
> 评估目的：为 VLA（Vision-Language-Action）训练流程选择最合适的仿真器
> 评估日期：2026-06-18

---

## 1. 执行摘要（Executive Summary）

综合硬件兼容性、迁移成本与训练效率三方面评估，**建议当前阶段继续使用 MuJoCo 作为主力仿真器**，同时将 ManiSkill3 作为并行实验环境进行小规模验证。Isaac Lab 因当前 GPU（GTX 1660 Ti，无 RT Cores）不满足最低硬件要求，**暂不推荐采用**。若未来升级至 RTX 3070 及以上显卡，可重新评估 Isaac Lab 作为大规模并行训练方案。

---

## 2. 评估环境（Evaluation Environment）

| 项目 | 规格 |
|------|------|
| GPU 型号 | NVIDIA GTX 1660 Ti |
| 显存（VRAM） | 6 GB |
| RT Cores | 无（不支持硬件光线追踪） |
| CUDA 支持 | 支持 |
| 操作系统 | Linux |
| 当前仿真器 | MuJoCo（已集成，运行正常） |

**关键约束**：6GB 显存与缺少 RT Cores 是限制 Isaac Lab 可用性的核心硬件因素。

---

## 3. ManiSkill3 评估（ManiSkill3 Evaluation）

### 3.1 安装与部署
- **版本**：v3.0.1，通过 pip 成功安装
- **PhysX GPU 库**：需手动下载 `libPhysXGpu_64.so`（约 236MB），否则无法启用 GPU 并行
- **整体安装难度**：中等（需额外处理 PhysX 库依赖）

### 3.2 性能表现
- **GPU 并行（纯状态观测）**：1024 个并行环境，约 14,800 FPS
- **GPU 并行（RGB 渲染）**：64 个并行环境
- **结论**：在纯状态输入场景下并行效率极高，适合大规模 RL 训练

### 3.3 Panda 机器人支持
- **内置任务**：`PickCube-v1`、`StackCube-v1`，使用 Franka Panda 模型
- **开箱即用**：无需自定义 MJCF 模型

### 3.4 API 兼容性
- **接口标准**：Gymnasium 兼容
- **关键差异**：GPU 并行模式下返回 `torch.Tensor`，而非 numpy dict，需在数据流转层做适配
- **动作空间**：9 维（7 关节 + 2 夹爪），与当前 MuJoCo 的 8 维（7 关节 + 1 夹爪）不一致

### 3.5 迁移成本评估
- **整体评级**：中等
- **主要工作**：需编写观测/动作适配器（adapter wrapper），处理 Tensor↔numpy 转换与动作空间维度差异

---

## 4. Isaac Lab 评估（Isaac Lab Evaluation）

### 4.1 安装状态
- **状态**：未安装
- **原因**：硬件要求未满足，无法进行实际测试

### 4.2 硬件要求
| 要求项 | Isaac Lab 最低要求 | 当前硬件 | 是否满足 |
|--------|---------------------|----------|----------|
| GPU 型号 | RTX 3070+ | GTX 1660 Ti | ❌ |
| 显存 | 8 GB | 6 GB | ❌ |
| RT Cores | 必需 | 无 | ❌ |

### 4.3 并行能力（理论值）
- **最大并行环境数**：在 RTX 4090 上可达 4096 个
- **优势**：大规模并行训练潜力最大

### 4.4 API 与生态
- **架构**：Manager-Based 架构，与 Gymnasium 差异较大
- **Panda 支持**：完整内置支持
- **迁移成本**：高（需重构环境交互逻辑）

### 4.5 结论
在当前硬件条件下，Isaac Lab 无法运行，**本次评估仅基于文档与官方说明，缺乏实测数据**。建议在硬件升级后重新评估。

---

## 5. 对比表（Comparison Table）

| 评估维度 | MuJoCo（当前） | ManiSkill3 | Isaac Lab |
|----------|----------------|------------|-----------|
| **物理精度（Physics Accuracy）** | 高精度，重力补偿正常工作，自定义 MJCF 模型可控性强 | 基于 PhysX，精度良好，GPU 模式下数值稳定性需验证 | 基于 PhysX 5，精度高，支持软体/流体等高级特性 |
| **训练速度（Training Speed）** | CPU 单环境约 500–1000 FPS，并行能力有限 | GPU 并行 1024 环境 ~14,800 FPS（状态），64 环境（RGB） | 理论最高，RTX 4090 可达 4096 并行环境 |
| **API 易用性（API Ease）** | Gymnasium 兼容（自定义 wrapper），已与 VLA 完全集成 | Gymnasium 兼容，但 GPU 模式返回 Tensor，动作空间 9D 需适配 | Manager-Based 架构，学习曲线陡峭，迁移成本高 |
| **GPU 要求（GPU Requirements）** | 无 GPU 硬性要求，CPU 即可运行 | 需 GPU + PhysX GPU 库（~236MB），GTX 1660 Ti 可用 | 必须 RTX 3070+（8GB + RT Cores），当前不可用 |
| **社区支持（Community Support）** | 成熟稳定，文档完善，机器人领域广泛使用 | 活跃发展中，文档较新，社区增长快 | NVIDIA 官方支持，文档完善，但生态较新 |

---

## 6. 推荐方案（Recommendation）

### 6.1 当前阶段（GTX 1660 Ti）
**首选：MuJoCo（继续使用）**
- 已与 VLA 流程完全集成（图像渲染、状态提取均正常）
- 物理精度满足需求，迁移风险最低
- 适合算法迭代与策略验证阶段

**备选：ManiSkill3（小规模并行实验）**
- 当需要加速纯状态 RL 训练时启用
- 利用其 1024 环境 / 14,800 FPS 的并行能力
- 仅作为实验性补充，不替代主流程

### 6.2 硬件升级后（RTX 3070+）
**重新评估 Isaac Lab**
- 若升级至 RTX 3070/4070/4090，可解锁 4096 并行环境
- 适合大规模分布式 RL 训练场景
- 需评估 Manager-Based 架构的迁移投入产出比

### 6.3 决策矩阵

| 使用场景 | 推荐仿真器 | 理由 |
|----------|------------|------|
| VLA 策略训练（当前） | MuJoCo | 已集成，零迁移成本 |
| 纯状态 RL 大规模并行 | ManiSkill3 | 1024 环境，14,800 FPS |
| RGB 多环境并行训练 | ManiSkill3 | 64 环境 GPU 渲染 |
| 硬件升级后大规模 RL | Isaac Lab | 4096 并行，性能上限最高 |
| 精细物理仿真验证 | MuJoCo | 高精度，重力补偿可靠 |

---

## 7. 迁移路径（Migration Path）

### 7.1 MuJoCo → ManiSkill3 迁移步骤

若决定将部分训练流程迁移至 ManiSkill3，建议按以下步骤进行：

#### 步骤 1：环境适配器开发（核心工作）
- **观测适配器**：将 ManiSkill3 GPU 模式返回的 `torch.Tensor` 转换为现有流程期望的 numpy dict 结构
- **动作适配器**：处理动作空间维度差异
  - 当前：8D = 7 关节 + 1 夹爪（连续值）
  - ManiSkill3：9D = 7 关节 + 2 夹爪（左右手指独立控制）
  - 方案：将 1 维夹爪信号复制为 2 维，或反向取均值

#### 步骤 2：任务映射
- 将当前 MuJoCo 自定义任务映射到 ManiSkill3 内置任务
  - `PickCube-v1` ← 对应当前 pick-place 任务
  - `StackCube-v1` ← 对应当前 stack 任务
- 验证初始状态分布、目标条件、奖励函数的一致性

#### 步骤 3：PhysX GPU 库部署
- 手动下载 `libPhysXGpu_64.so`（~236MB）
- 配置环境变量，确保 GPU 并行模式可正常加载

#### 步骤 4：VLA 集成验证
- 图像渲染接口对接（确认 RGB 观测格式与当前 VLA 输入一致）
- 状态提取接口对接（关节角度、夹爪状态、物体位姿）
- 端到端训练流程跑通验证

#### 步骤 5：性能基准测试
- 对比 MuJoCo（CPU 单环境）与 ManiSkill3（GPU 并行）在相同任务下的：
  - 样本吞吐量（FPS）
  - 训练收敛速度
  - 最终策略性能

#### 步骤 6：渐进式切换
- 先在纯状态 RL 实验中采用 ManiSkill3
- 验证稳定后，再扩展到 RGB 多环境并行训练
- 保留 MuJoCo 作为精细验证与回归测试环境

### 7.2 迁移风险与注意事项

| 风险点 | 影响 | 缓解措施 |
|--------|------|----------|
| 动作空间维度不一致（8D vs 9D） | 策略输出层需调整 | 编写可配置维度的动作适配器 |
| Tensor vs numpy 返回格式 | 数据流需重构 | 在 wrapper 层统一转换为 numpy |
| PhysX 与 MuJoCo 物理差异 | 策略跨仿真器迁移可能有 sim-to-sim gap | 双环境回归测试，验证策略泛化性 |
| PhysX GPU 库手动下载 | 部署流程复杂 | 固化下载脚本，写入部署文档 |

---

## 附录：评估数据来源

- **ManiSkill3**：v3.0.1 实测安装与基准测试
- **Isaac Lab**：基于官方文档与硬件要求说明（未实测）
- **MuJoCo**：当前生产环境实际运行数据

---

*本报告基于 2026-06-18 评估结果生成。若硬件配置或仿真器版本发生变化，建议重新评估。*
